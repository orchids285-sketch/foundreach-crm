// A gallery image displayed on the application marketplace page. `path` is a
// path into the app `public/` folder; `position` is the display order.
export type GalleryImageManifest = {
  path: string;
  position: number;
};
